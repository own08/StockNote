# PC・スマホで同じデータを使うための初期設定

公開先リポジトリ： https://github.com/own08/StockNote

アプリの画面はGitHub Pages、在庫データとログインはSupabaseに保存します。GitHubへ商品の実データやバックアップはアップロードしません。

## 1. 現在のデータを保存

これまで使っていたローカル版を開き、「データ管理」→「全データをバックアップ」でJSONを保存してください。**ブラウザにある在庫データは、GitHubにファイルを置いても自動移行されません。**

## 2. Supabaseでプロジェクトを作成

1. https://supabase.com/dashboard でアカウントを作成・ログインします。GitHubアカウントでの登録も利用できます。
2. 新しいプロジェクトを作成します。名前の例は `StockNote`。地域は利用場所に近いものを選びます。
3. データベース用パスワードを設定し、ご自身で安全に保管します。このパスワードはアプリのログイン用とは別です。コードやチャットに貼る必要はありません。
4. 利用プランと表示される条件を確認して作成します。費用の発生する変更はご自身で確認してください。

## 3. 在庫の保存先とアクセス制限を作成

1. Supabaseプロジェクトの **SQL Editor** を開きます。
2. このリポジトリの `supabase/setup.sql` の内容をすべて貼り付けて実行します。
3. 成功したことを確認します。`stock_notes` テーブルと、本人のデータだけにアクセスするRLSポリシー、同時更新を検知する保存関数を作成します。

このSQLはアプリの在庫データを含みません。既存の `stock_notes` を削除する処理もありません。

## 4. ご自身のログインアカウントを作成

1. Supabaseの **Authentication → Users** からユーザーを追加します。`Add user` / `Create new user` に相当する操作を選びます。
2. このアプリで使うメールアドレスとパスワードを指定します。メール確認済み（Auto Confirm User）の設定がある場合は、ご自身のアドレスで有効にして作成します。
3. **Authentication の Sign In / Providers 設定**でメール・パスワード認証を有効にし、**Allow new users to sign up** を無効にしてください。個人用なのでアプリに新規登録フォームは用意していません。
4. PC・スマホでは、ここで作った**同じメールアドレス・パスワード**を使います。別のユーザーを作るとデータも別になります。

Supabase管理画面へGitHubでログインすることと、Stock Noteにメールでログインすることは別です。パスワードを忘れた場合はSupabase側でユーザーのパスワードを再設定してください。メール配信の設定が必要なセルフサービスのパスワード再発行画面は今回含めていません。

## 5. 公開してよい接続情報を設定

Supabaseプロジェクトの **Connect** 画面で以下を確認します。キーは **Settings → API Keys** でも確認できます。

- Project URL：`https://xxxxxxxx.supabase.co` の形式
- Publishable key：`sb_publishable_...` で始まる公開用キー

GitHubで `config.js` を開き、鉛筆アイコンなどで編集して、この2つを入力します。

```js
window.STOCK_CONFIG = {
  mode: 'cloud',
  supabaseUrl: 'https://xxxxxxxx.supabase.co',
  supabasePublishableKey: 'sb_publishable_ここに公開用キー'
};
```

**Secret key（`sb_secret_...`）・service_roleキー・データベースのパスワードは入力しないでください。** Publishable keyはブラウザから使う公開用のキーです。在庫データはユーザー認証と手順3のRLSで保護します。

## 6. GitHub Pagesを有効にする

1. https://github.com/own08/StockNote/settings/pages を開きます。
2. **Build and deployment → Source** で **GitHub Actions** を選びます。
3. **Actions** タブで **Publish Stock Note** を開きます。必要なら **Run workflow** を押して `main` を指定して実行します。
4. 成功後、Pages画面に表示されるURLを開きます。通常は https://own08.github.io/StockNote/ です。公開URLはPages画面で確定してください。

`main` に変更を保存すると、その後は自動で公開版が更新されます。GitHubのプランやリポジトリの公開範囲によってPagesの利用条件が異なる場合は、GitHub画面の案内を確認してください。

## 7. 元の在庫データを移行

1. 公開URLをPCで開き、手順4のアカウントでログインします。
2. 「データ管理」→「JSONを復元」で手順1のJSONを選びます。
3. 件数を確認し、復元を実行します。**同じアカウントの共有データ全体を置き換える操作です。**
4. 上部に **同期済み** と表示されるまで待ちます。
5. スマホで同じURLを開き、同じアカウントでログインします。商品・週次履歴・発注・並び順が同じになります。

ローカル版の保存データは削除しません。移行確認後も元のJSONは保管しておくと安心です。

## 8. 実際の接続確認

本番のプロジェクト作成後に、次を確認してください。

- PCで商品を1件追加 → スマホで「最新に更新」→ 表示される。
- スマホで数値を更新 → 同期済みを確認 → PCで「最新に更新」→ 同じ数になる。
- 商品の順番を変更して、別端末にも反映される。
- 発注を一部入荷・全量入荷し、現在庫と処理済み数を両端末で確認する。
- ログアウト後は在庫が表示されず、再ログインすると復元される。

共有先の実アカウントが未設定の段階では、実際のクラウド接続・公開は完了していません。

## 同期の動き

- 操作するとすぐに共有保存を開始します。「送信中」と「同期済み」を区別します。送信中は次の編集を一時停止します。
- 開いている画面は約12秒ごと、または画面に戻ったときに更新を確認します。「最新に更新」も使えます。フォーム・数値欄を編集中は勝手に画面を差し替えません。
- ほかの端末が先に保存していた場合、自動で上書きせず競合を表示します。「未送信JSONを保存」で変更案を残せます。その後「共有データを読み直す」で最新を確認し、必要な変更を入れ直します。
- 通信失敗は「再試行」で同じ保存要求を再送します。応答だけが途切れた場合も、同じ要求の再送による入荷の二重加算を防ぎます。別の端末がさらに更新した場合は競合になります。変更がすでに反映されている場合もあるため、最新データを確認してください。
- 未送信データは**そのタブのsessionStorage**に保持します。再読み込み後に復旧できますが、タブを閉じると消える可能性があります。通信失敗時は未送信JSONを保存してから閉じてください。
- ログイン状態もそのタブに保持します。タブを閉じた後は再ログインが必要になる場合があります。
- クラウド版はオンライン利用が前提です。オフラインで編集をためて自動統合する機能はありません。

## 補足

- GitHubのコード・公開用接続情報は公開されても、在庫本体はGitHubに保存されません。
- 1アカウントにつき1つの個人在庫ノートです。異なるアカウント間の共同編集は対象外です。
- ブラウザから直接SupabaseのAPIを使うため、独自サーバー・npmインストールは不要です。
- `config.js` の `mode` を `'local'` にすると従来のローカル保存に戻せます。このモードでは同期しません。

## 公式情報

- GitHub Pagesの公開：https://docs.github.com/en/pages/getting-started-with-github-pages/creating-a-github-pages-site
- Supabase公開用キー：https://supabase.com/docs/guides/getting-started/api-keys
- アクセス制限（RLS）：https://supabase.com/docs/guides/database/postgres/row-level-security
- サインアップの制限：https://supabase.com/docs/guides/auth/general-configuration
